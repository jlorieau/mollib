from mollib.utils import Datum

class RDC(Datum):
    """Residual Dipolar Coupling (RDC) data point."""
    id_str = None

class RACS(Datum):
    """Residual Anisotropic Chemical Shift (RACS) data point."""
    id_str = None